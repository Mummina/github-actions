name: build release

on:
  push:
    branches:
      - "master"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: checkout code
        uses: action/checkout@v3
      - name: steup Node.js
        uses: action/setup-node@v3
      - name: install dependencies
        run: npm ci
      - name: build windows packges
        run: |
          ls 
          npm run build:windows

      
        
