name: build release

on:
  push:
    branches:
      - "master"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: action/checkout@v3
      - uses: action/setup-node@v3
        with:
          node-version: '14'
      - run: |
          ls 
          pwd
          npm install package 
          npm build:windows    

      
      
        
