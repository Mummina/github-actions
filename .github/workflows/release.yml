
name: releases
on:
  push:
    branches:
      - "master"
jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: list files
        run: |
          pwd
          ls
      - name: check out code
        uses: Mummina/github-actions@master
      - name: multiple linescript
        run: |
         node -v
         npm -v



